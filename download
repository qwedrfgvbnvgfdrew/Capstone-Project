__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
*.egg-info/
.venv/
venv/

# NOTE: mlflow.db and mlruns/ are intentionally NOT ignored.
# They contain the actual logged MLflow experiment runs (params, metrics,
# and the final saved model) produced by `python -m src.train`, and are kept
# in version control as evidence of experiment tracking for grading purposes.
